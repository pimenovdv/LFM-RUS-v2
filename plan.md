1. **Обновление todo.md:**
   - Сжать существующие выполненные пункты (Шаги 1-8) в один короткий пункт.
   - Добавить новый запланированный пункт (Шаг 9) для добавления поддержек `frequency_penalty` и `presence_penalty` в процесс генерации `DiffusionModelForConditionalGeneration`. Этот пункт сразу будет помечен как выполненный после реализации в рамках этого же PR (или будет отмечен после тестов).

2. **Изменение `src/models/diffusion/modeling_diffusion.py`:**
   - Добавить аргументы `frequency_penalty: float = 0.0` и `presence_penalty: float = 0.0` в метод `generate`.
   - Внутри цикла по блокам и шагам, перед сэмплированием токенов, добавить логику применения штрафов: вычисление уникальных токенов и их частот в контексте, после чего вычитание `counts * frequency_penalty` и `presence_penalty` из логитов соответствующих токенов.

3. **Написание тестов:**
   - Добавить тест `test_generate_frequency_presence_penalty` в `tests/test_mdlm_generation.py`, чтобы покрыть новые параметры генерации.

4. **Предкоммитные проверки (`pre-commit`):**
   - Выполнить все необходимые предкоммитные проверки для уверенности в качестве кода (тесты, линтеры и т.д.).

5. **Коммит и отправка изменений:**
   - Закоммитить изменения с подходящим сообщением и выполнить submit на текущую или новую ветку.
